apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: stac
  namespace: stac
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`stac.soulsbros.ch`)
      kind: Rule
      services:
        - name: stac
          port: 80
